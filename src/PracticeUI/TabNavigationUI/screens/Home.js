import { View, Text } from "react-native";
import React from "react";

const Home = () => {
    return (
        <View
            style={{
                backgroundColor: '#fff',
                flex: 1,
                paddingTop: 40,
            }}>
            <Text style={{
                //color: '#000'
            }}>Home</Text>
        </View>
    );
};

export default Home;

import { View, Text } from "react-native";
import React from "react";

const Settings = () => {
    return (
        <View
            style={{
                flex: 1,
                paddingTop: 40,
                backgroundColor: '#fff'
            }}>
            <Text>Settings</Text>
        </View>
    );
};

export default Settings;
